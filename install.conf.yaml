- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: 
      force: true
      path: bash/bashrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
